# MindLog Docs

This folder is intended for a **static documentation site** deployed via CI/CD.

Suggested setup:

1. Pick a generator (recommended: VitePress)
2. Build static output in CI
3. Deploy to GitHub Pages / Cloudflare Pages / Netlify / Vercel

If you want, I can scaffold VitePress here (`docs/` as the site root) with:

- `docs/package.json`
- `docs/.vitepress/config.ts`
- `docs/index.md`
- build + deploy instructions

